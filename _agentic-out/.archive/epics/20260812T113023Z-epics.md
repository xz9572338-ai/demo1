---
stepsCompleted:
  - 1
  - 2
  - 3
  - 4
status: "complete"
completedAt: "2026-08-12"
inputDocuments:
  - "_agentic-out/planning/prd.md"
  - "_agentic-out/planning/architecture.md"
  - "_agentic-out/planning/ux-design-specification.md"
  - "_agentic-out/reviews/2026-08-12-readiness.md"
---

# 轻量化开放平台 - Epic 与 Story 拆分

## 概述

本文档将 PRD、UX 设计和架构要求分解为可实施的 Epic 与 Story，并保持完整需求追踪。

## 需求清单

### 功能需求

FR1: 外部客户可以使用企业名称、联系人和手机号提交企业入驻申请。
FR2: 平台可以为入驻申请维护待审核、通过和驳回状态。
FR3: 外部客户可以查看当前入驻状态及驳回原因。
FR4: 入驻被驳回的客户可以修改企业名称、联系人和手机号并重新提交；提交成功后申请由“驳回”转为“待审核”，已通过或待审核申请不得重复提交。
FR5: 只有入驻审核通过的客户可以使用应用、权限申请、调试和统计功能。
FR6: 外部客户可以使用账号和密码建立登录会话；退出后当前会话令牌立即失效，再次访问受保护页面必须重新认证。
FR7: 同一账号 15 分钟内连续登录失败 5 次后锁定 30 分钟；同一 IP 15 分钟内失败 20 次后拒绝该 IP 新登录 30 分钟，人工密码重置可同时解除账号锁定。
FR8: 内部责任人可以通过受控流程为客户执行人工密码重置。
FR9: 商务专员可以记录入驻审核结果、原因和审核信息。
FR10: 审核通过的客户可以创建对接应用。
FR11: 平台可以为每个应用生成唯一 AppID 和 AppSecret。
FR12: 客户只能在 AppSecret 创建或重置后查看一次明文密钥。
FR13: 客户可以查看本企业创建的应用及其状态。
FR14: 客户可以自主停用应用。
FR15: 客户可以重置应用密钥。
FR16: 密钥重置后新密钥立即生效，旧密钥在 24 小时轮换窗口内继续有效；客户可提前终止窗口，窗口结束或提前终止后使用旧密钥的请求必须被拒绝。
FR17: 客户不能访问其他企业的应用或凭证信息。
FR18: 平台可以分别管理应用的沙箱凭证和生产凭证。
FR19: 客户可以按应用查看可申请的开放接口清单。
FR20: 客户可以为应用逐接口提交权限申请。
FR21: 客户可以查看每项接口权限申请的待审核、通过或驳回状态及驳回原因。
FR22: 业务产品可以记录接口权限审批结果、原因和审核信息。
FR23: 平台只允许应用调用已获批准的接口。
FR24: 应用停用后拒绝该应用全部环境调用；账号失效后拒绝其企业全部应用调用；接口权限撤销后拒绝该应用对该接口的新请求，状态变更须在 1 分钟内生效。
FR25: 技术对接负责人可以记录客户身份绑定、联调结果和生产上线验收结论。
FR26: 平台只为完成生产验收的应用启用生产访问。
FR27: 平台可以维护企业账号、应用、凭证、接口权限与业务客户主数据标识之间的关联关系。
FR28: 客户可以浏览客户基础信息、订单列表和订单详情接口的文档。
FR29: 客户可以查看接口用途、访问地址、权限要求、请求参数、响应字段和示例。
FR30: 客户可以查看签名规则、规范化请求示例、时间偏差和防重放要求。
FR31: 客户可以查看分页、增量查询、限流、重试和错误码说明。
FR32: 客户可以在页面向沙箱环境发起接口调试请求。
FR33: 页面在线调试不能向生产环境发送请求。
FR34: 文档、沙箱和生产接口的路径、参数、字段、校验、分页和错误语义必须与同一版本的 OpenAPI 契约一致，不一致项为 0。
FR35: 客户可以取得至少一种可复现的签名和调用示例。
FR36: 获得权限的应用可以查询当前绑定客户范围内的客户基础信息。
FR37: 获得权限的应用可以按时间范围或更新时间增量查询当前客户的订单列表。
FR38: 获得权限的应用可以按 `updatedAt ASC, orderId ASC` 使用游标分页遍历订单；后续页从上一页末游标之后读取，并发更新不得造成满足固定查询条件的订单永久遗漏或重复计入。
FR39: 获得权限的应用可以查询当前客户范围内的订单详情。
FR40: 平台可以拒绝查询不属于当前客户的数据资源。
FR41: 平台以数据中台为客户和订单权威来源；订单列表与详情的 `orderId`、`customerId`、`status`、`orderAmount`、`createdAt`、`updatedAt` 及脱敏结果必须一致，允许的同步延迟不超过 5 分钟。
FR42: 数据中台请求超过 3 秒、连接失败或数据延迟超过 5 分钟时，平台必须返回对应的 `DOWNSTREAM_TIMEOUT`、`DOWNSTREAM_UNAVAILABLE` 或 `DATA_STALE` 错误及 Request ID，不得以成功响应返回部分或过期结果。
FR43: 对外响应中的手机号仅保留前三位和后四位；收货地址仅保留省、市，区县、街道、门牌号及收件细节替换为 `***`，适用于 Customer.contactMobile、OrderDetail.receiverMobile 和 OrderDetail.receiverAddress。
FR44: 平台不向应用提供任何生产业务数据写入能力。
FR45: 平台可以根据 AppID 和请求签名识别并验证调用应用。
FR46: 平台可以拒绝签名无效、请求过期或重复使用防重放标识的请求。
FR47: 平台可以同时校验账号、应用、环境和接口权限状态。
FR48: 平台可以按应用和接口限制调用频率。
FR49: 超过应用与接口额度时，平台必须返回 HTTP `429`、业务码 `RATE_LIMITED`、Request ID 和非负整数秒的 `Retry-After`。
FR50: 经业务产品批准的客户额度调整必须记录客户、应用、接口、原额度、新额度、生效时间、失效时间、申请原因、操作人和复核人；未批准或过期配置不得生效。
FR51: 单一应用持续达到其限流上限或产生突发流量时，其他客户仍须满足生产 API P95≤1 秒、月度可用性≥99.9%和平台原因失败率≤1%，且传递至数据中台的总流量不得超过 3,000 RPM。
FR52: 客户可以按应用查看近 7 天接口调用总量、成功数和失败数。
FR53: 客户可以按接口筛选近 7 天调用统计。
FR54: 平台可以区分鉴权失败、权限拒绝、限流、业务错误、超时和系统错误。
FR55: 平台可以为每次接口调用生成唯一 Request ID。
FR56: 技术对接负责人和运维可以根据 Request ID、应用、接口、环境和时间定位内部调用记录。
FR57: 技术对接负责人可以记录客户问题、排查过程、处置结果和反馈状态。
FR58: 平台可以按企业和业务客户标识隔离账号、应用、权限、调用记录和业务数据。
FR59: 每次业务数据访问均必须校验资源所属客户是否在已鉴权应用的授权客户范围内；越权请求一律拒绝并产生审计记录，跨客户数据返回数为 0。
FR60: API 访问日志、应用日志和错误日志不得记录 AppSecret、完整签名、完整手机号、完整收货地址或内部堆栈；手机号和地址如需记录必须使用 FR43 的脱敏结果。
FR61: 平台必须记录入驻审批、权限审批、生产验收、密码人工重置、密钥重置、应用停用或启用、权限撤销和额度调整操作，不得以开放式类别替代上述审计范围。
FR62: 审计记录可以标识操作对象、操作人、复核人、变更前后状态、原因和时间。
FR63: 授权内部人员可以查询审计记录并追溯关键状态变更。
FR64: 平台可以分别管理生产与沙箱的数据、访问权限和操作记录。
FR65: 平台可以依据已批准的字段开放清单限制接口输出数据范围。

### 非功能需求

NFR1: 在 20 个生产应用、整体 3,000 RPM 且各接口每月有效样本不少于 1,000 的负载下，三个查询接口各自 P95 服务端响应时间不得超过 1 秒，不含客户网络耗时。
NFR2: 在 50 个并发控制台会话下，登录、应用列表、权限状态和调用统计查询的 P95 服务端响应时间不得超过 2 秒；按每次发布后的验收记录判定。
NFR3: 三个查询接口访问数据中台的单次超时为 3 秒；超时后 1 秒内须返回 `DOWNSTREAM_TIMEOUT` 和 Request ID，适用于沙箱及生产客户调用。
NFR4: 订单列表单次查询时间范围不得超过 31 天，默认页大小为 100、最大为 500，并按 `updatedAt ASC, orderId ASC` 返回；超限请求必须返回参数错误且不访问数据中台。
NFR5: 在 20 个并发应用各以每分钟 60 次请求、页面大小 500、查询范围 31 天运行 30 分钟时，系统仍须满足 NFR1、NFR7 和 NFR29。
NFR6: 生产 API 每自然月可用性不得低于 99.9%，按 `1-非计划不可用分钟/当月总分钟` 计算；仅排除提前 3 个工作日通知且每月累计不超过 4 小时的计划维护。
NFR7: 每自然月平台 `5xx` 和平台超时请求数占进入平台的有效生产请求数不得超过 1%；客户参数、鉴权、权限和主动限流错误不计入分子或有效请求分母。
NFR8: 核心服务发生故障后，恢复时间目标 RTO 不得超过 4 小时。
NFR9: 核心平台数据的恢复点目标 RPO 不得超过 1 小时。
NFR10: 单个应用以 5 倍默认额度持续请求 30 分钟时，其他客户仍须满足 P95≤1 秒、可用性≥99.9%折算目标及平台原因失败率≤1%。
NFR11: 数据中台不可用或数据不完整时，平台不得将不完整结果伪装为成功响应。
NFR12: 同一 API 版本下，沙箱和生产的字段、必填性、校验、签名、分页及错误语义与 OpenAPI 契约的不一致项必须为 0。
NFR13: 控制台、沙箱和生产 API 的所有网络通信必须使用 HTTPS。
NFR14: AppSecret、手机号和收货地址在数据库、缓存持久化和备份中必须使用不低于 AES-256 强度的加密保护；密钥不得与密文存放于同一配置或数据存储中。
NFR15: AppSecret、完整签名、完整手机号、完整收货地址和内部错误堆栈不得写入 API 访问日志、应用日志或错误日志。
NFR16: 手机号在对外响应和展示中仅保留前三位和后四位；收货地址仅保留省、市，其他地址信息统一替换为 `***`，适用字段以 v1 数据契约为封闭清单。
NFR17: 客户数据访问必须同时通过应用身份、接口权限和业务客户归属校验。
NFR18: 任意访问非授权客户资源的请求必须被拒绝并记录客户、应用、资源、时间和 Request ID；跨客户数据返回事件必须为 0。
NFR19: 请求签名必须具备时间窗口和防重放保护；重复 nonce 和过期请求必须被拒绝。
NFR20: 密钥明文只能在创建或重置时展示一次，之后任何用户和普通运维人员均不能直接查看。
NFR21: 登录锁定、审核、人工密码重置、密钥重置、应用状态、权限、生产启用和限流调整必须记录操作对象、操作人、复核人、前后状态、原因、时间和 Request ID（适用时），必填字段完整率为 100%。
NFR22: 平台仅在中国境内处理和存储业务数据，不得向境外系统传输。
NFR23: 产品负责人、业务产品和技术对接负责人须在生产启用前共同批准数据分类分级、字段开放清单、个人信息处理目的和留存规则；批准记录及版本号是生产启用的必备证据。
NFR24: 一期平台必须支持至少 20 个同时启用且各自可按默认 60 RPM 额度调用的生产应用。
NFR25: 平台必须支持整体每分钟至少 3,000 次 API 请求，并在该负载下继续满足性能和失败率指标。
NFR26: 当传递至数据中台的总请求达到 2,400 RPM（一期 3,000 RPM 容量的 80%）时，平台必须先对超出应用额度的请求返回 `429`；数据中台入口流量不得超过 3,000 RPM。
NFR27: 新增只读查询接口不得引入第二套账号、凭证、权限、限流、隔离、审计或统计模型；上述七类公共能力的复用覆盖率必须为 100%。
NFR28: 应用与接口维度的限流额度必须可独立调整，且调整过程不得要求中断其他客户服务。
NFR29: 数据从数据中台可用到开放 API 可查询的延迟 P95 不得超过 5 分钟。
NFR30: 平台须每分钟监测数据中台失败率、P95 耗时和数据延迟；指标异常后 2 分钟内在内部监控中标明数据中台或平台来源，覆盖三个开放接口。
NFR31: 每日抽取不少于 100 个订单（不足时全量）比对列表和详情的 `orderId`、`customerId`、`status`、`orderAmount`、`createdAt`、`updatedAt` 及脱敏结果，不一致率必须为 0。
NFR32: 增量查询在相同排序条件下不得因分页过程造成符合条件的数据永久遗漏。
NFR33: 数据中台字段或枚举变化不得删除 v1 必填字段、改变既有字段类型或改变既有枚举语义；不兼容变化必须通过新的 API 主版本提供。
NFR34: 每次 API 调用必须具有全链路唯一 Request ID。
NFR35: 调用日志必须支持按 Request ID、应用、接口、环境、时间和错误分类检索。
NFR36: 用于问题排查的调用日志至少保留 90 天。
NFR37: FR61 所列全部操作的审计日志自发生时间起至少保留 1 年，并在保留期内可按操作对象、操作人和时间检索。
NFR38: 调用统计采用中国标准时间（UTC+8）自然日，每小时刷新一次，调用发生至控制台可见的延迟不得超过 2 小时；HTTP `2xx` 且业务码成功计为成功，其余终态请求计为失败，同一 Request ID 仅计一次。
NFR39: 日志和监控组件故障不得绕过鉴权、授权或数据隔离控制。
NFR40: 客户 Web 控制台必须支持 Chrome 和 Edge 最新两个主要版本。
NFR41: 一期不要求移动端优先、搜索引擎优化或旧版浏览器兼容。
NFR42: 注册、登录、创建应用、复制首次密钥、提交权限申请、查看文档、发起沙箱调试、重置密钥和查看统计九项流程必须仅用键盘完成；所有输入有程序化标签，状态与错误同时提供文本名称、原因和下一步动作，不得仅靠颜色表达。

### 架构附加要求

AR1: 第一条实施 Story 必须初始化 Java 21 + Spring Boot 4.1、React + TypeScript + Vite + shadcn/ui 的单仓库结构。
AR2: 后端采用模块化单体，并按 identity、application、credential、permission、admission、supplychain、sandbox、statistics、audit 与 shared 划分边界。
AR3: MySQL 使用 LTS 系列，所有结构变化通过 Flyway；依赖数据库行为的测试使用 MySQL Testcontainers。
AR4: Redis 分别承载控制台会话、nonce、防暴力破解、限流及短期权限缓存，并为每类用途设置独立键前缀、TTL、指标与失败策略。
AR5: 控制台使用服务端 Cookie 会话与 CSRF 防护；开放 API 使用 AppID + HMAC-SHA256、±5 分钟时间窗口及 nonce 防重放。
AR6: AppSecret 使用信封加密和托管密钥管理，明文不得进入日志、URL、前端持久化状态或普通运维入口。
AR7: OpenAPI 设计文件是控制台 API、生产 API、沙箱 API、前端客户端、文档、模拟数据和契约测试的唯一来源。
AR8: 数据中台通过同步 HTTP/REST 防腐层访问；请求具备超时预算、有限重试、熔断、并发隔离、错误映射与 Request ID 传播。
AR9: 订单与客户业务事实不得复制到平台 MySQL；数据中台异常时不得使用陈旧缓存伪装成功。
AR10: 调用事件异步聚合；影响统计或审计持久性的事件使用事务 Outbox，消费者必须幂等。
AR11: API、沙箱、生产与内部端点使用明确路径边界；沙箱和生产使用独立域名、凭证、数据、权限、Redis 命名空间和密钥。
AR12: 所有模块从验证后的安全上下文取得企业及客户范围，禁止信任请求中的 customerId 作为授权依据。
AR13: CI 必须覆盖 OpenAPI 校验与破坏性变更、生成代码一致性、Flyway 校验、模块边界、安全扫描、测试和镜像构建。
AR14: 部署采用云厂商中立的托管容器、MySQL、Redis、KMS、日志监控与 OpenTelemetry；API 服务保持无状态并可水平扩展。
AR15: 普通调用日志与审计日志分离，结构化日志贯穿 requestId/traceId，且提供数据中台、Redis、凭证泄露和回滚运行手册。
AR16: 一期受控审核工具必须调用合法状态机并形成完整审计，不得任意更新业务表。

### UX 设计要求

UX-DR1: 实现青灰主题语义令牌，包含 background、foreground、card、primary、muted、accent、destructive、border、input、ring、sandbox 与 production。
UX-DR2: 桌面端采用 224px 一级侧栏和单内容工作区，平台总览突出接入进度、下一步、应用及近七天调用概况。
UX-DR3: 实现 OnboardingTracker，覆盖未开始、进行中、待审核、驳回、阻塞、完成，并使用 aria-current 标识当前步骤。
UX-DR4: 实现 ReviewStatusPanel，统一展示入驻、权限和生产验收的状态、时间、责任角色、原因、下一步及反馈渠道。
UX-DR5: 实现 OneTimeSecretReveal，覆盖复制、确认保存、复制失败和离开风险；复制结果必须可被辅助技术感知。
UX-DR6: 实现 SecretRotationPanel，显示24小时轮换窗口、旧密钥失效时间、新密钥验证和提前失效危险操作。
UX-DR7: 实现 PermissionMatrix，逐接口展示用途、敏感提示、申请原因、状态与驳回原因。
UX-DR8: 实现 SandboxRequestRunner，始终标明沙箱环境且不存在生产切换入口，覆盖校验、发送、成功、业务失败、网络失败和超时。
UX-DR9: 实现 ResponseInspector，展示 HTTP 状态、业务码、耗时、Request ID、JSON 响应、复制操作和修复建议。
UX-DR10: 实现 DangerActionDialog，普通危险操作二次确认，高影响操作输入应用名称；初始焦点在取消按钮且关闭后恢复焦点。
UX-DR11: 页面最多一个主按钮；次要与危险动作使用统一层级，禁用动作就近解释原因。
UX-DR12: 表单标签始终可见，错误与控件关联，提交失败保留内容，错误摘要可定位首个错误字段。
UX-DR13: 桌面导航包含总览、应用管理、接口权限、API 文档、调用统计；移动端转换为 Sheet。
UX-DR14: 沙箱与生产同时使用语义色和明确文字，不得仅靠颜色、域名或凭证前缀表达环境。
UX-DR15: 加载、空、错误、成功、选中、禁用和危险操作状态在所有关键业务组件中保持一致。
UX-DR16: 调用统计默认近7天，展示总量、成功、失败和成功率，并支持按接口筛选；图表须提供文字摘要和精确数据。
UX-DR17: 移动端将表格转换为带字段标签的记录列表，不以横向压缩隐藏列名。
UX-DR18: 响应式断点为 320–639、640–767、768–1023、1024–1279 与 1280px 以上，并按规格切换导航、表格和调试布局。
UX-DR19: 移动端完整保留状态查看、首次密钥复制、密钥应急轮换、应用停用、错误诊断和调用概况。
UX-DR20: 达到 WCAG 2.2 AA；正常文本对比度至少4.5:1，非文字控件和焦点至少3:1。
UX-DR21: 九项关键流程必须仅用键盘完成，提供跳至主要内容、清晰焦点、浮层焦点管理和动态状态播报。
UX-DR22: 触控目标至少44×44px；200%缩放无内容丢失；尊重 prefers-reduced-motion。
UX-DR23: 支持 Chrome 与 Edge 最近两个主要版本，并在 320、390、768、1024、1280、1440px 验证布局。
UX-DR24: 前端采用 React Router 数据路由、TanStack Query、React Hook Form 和 OpenAPI 生成客户端；敏感值不得进入 Query Cache、URL 或 localStorage。

### FR 覆盖映射

| FR 范围 | Epic | 覆盖主题 |
|---|---|---|
| FR1–FR9、FR58、FR60–FR63 | Epic 1 | 注册、入驻、登录、租户隔离、审计与日志避敏基线 |
| FR10–FR18、FR64 | Epic 2 | 应用、AppID、AppSecret、停用、轮换和环境隔离 |
| FR19–FR24、FR27、FR65 | Epic 3 | 权限申请、审批、身份绑定、字段批准和生产准备 |
| FR28–FR35 | Epic 4 | API 文档、签名、分页、错误码、沙箱调试和契约一致 |
| FR25–FR26、FR36–FR51、FR59 | Epic 5 | 三个查询接口、生产验收、数据归属、脱敏、签名和流量治理 |
| FR52–FR57 | Epic 6 | 调用统计、结果分类、Request ID 和问题闭环 |

覆盖结果：65/65，无遗漏、无重复归属。

## Epic 列表

### Epic 1：客户完成企业入驻并安全登录

外部客户能够注册企业账号、查看审核结果、修正驳回申请并建立安全登录会话；内部商务人员能够通过受控流程完成审核和密码重置。平台同时建立后续能力共用的租户隔离、审计和日志避敏基线。

**覆盖：** FR1–FR9、FR58、FR60–FR63

### Epic 2：客户创建并安全管理对接应用

审核通过的客户能够创建应用、取得 AppID 和仅首次展示的 AppSecret、区分沙箱与生产凭证、停用应用并安全轮换密钥；沙箱与生产的运行边界保持隔离。

**覆盖：** FR10–FR18、FR64

### Epic 3：客户申请接口权限并完成生产准备

客户能够查看接口清单、逐接口申请权限、获知审核状态与原因；内部角色能够审批权限、绑定业务客户主数据、批准字段开放范围并形成可供最终生产验收使用的准备状态。

**覆盖：** FR19–FR24、FR27、FR65

### Epic 4：客户依据统一文档完成沙箱联调

客户能够浏览三个接口的完整契约、签名、分页、限流、重试及错误说明，取得可复现示例，并通过页面向沙箱发起测试请求。

**覆盖：** FR28–FR35

### Epic 5：客户安全获取供应链查询数据

获准应用能够查询客户资料、订单列表与订单详情，使用稳定增量分页同步分钟级数据；平台正确处理数据归属、数据中台故障、数据脱敏、签名、防重放、权限和流量治理。接口及沙箱证据完整后，技术负责人完成最终生产验收并启用生产访问。

**覆盖：** FR25–FR26、FR36–FR51、FR59

### Epic 6：客户与技术团队掌握调用情况并完成故障闭环

客户能够查看近七天调用统计并按接口筛选；平台对调用结果分类并生成 Request ID；技术负责人和运维能够定位记录、处理问题并记录反馈结果。

**覆盖：** FR52–FR57

## Epic 1：客户完成企业入驻并安全登录

客户能够申请入驻、掌握审核状态并安全登录；商务人员能够通过受控流程审核，内部责任人能够处理密码重置。

### Story 1.1：初始化可持续交付的项目基线

作为交付团队，
我希望获得符合既定架构的可运行项目骨架，
以便后续功能能够在统一契约、模块边界和质量门槛上增量开发。

**Acceptance Criteria:**

**Given** 当前仓库尚无应用代码
**When** 初始化项目
**Then** 仓库包含 Java 21 + Spring Boot 4.1 的 `apps/api` 和 React + TypeScript + Vite + shadcn/ui 的 `apps/web`
**And** 前后端目录符合架构定义，Maven Wrapper、pnpm workspace、本地环境示例和 README 可用。

**Given** 开发者启动本地依赖
**When** 执行约定的本地启动命令
**Then** MySQL、Redis 和数据中台模拟服务可通过容器启动
**And** 后端健康检查和前端空壳页面可访问，且本地数据不含真实客户资料。

**Given** 代码提交到 CI
**When** 流水线运行
**Then** 至少执行后端编译与测试、前端类型检查与构建、OpenAPI 校验和 Flyway 校验
**And** 任一步失败均阻止合并。

### Story 1.2：客户提交企业入驻申请

作为外部合作客户，
我希望使用企业及联系人信息提交入驻申请，
以便获得开放平台使用资格。

**Acceptance Criteria:**

**Given** 访客进入注册页面
**When** 填写企业名称、联系人、手机号、账号和密码并提交有效信息
**Then** 系统创建企业账号和状态为“待审核”的入驻申请
**And** 页面显示提交时间、处理渠道和下一步，且禁止使用受保护功能。

**Given** 必填字段为空、手机号无效或账号已存在
**When** 客户提交
**Then** 系统不创建申请
**And** 就近显示错误并保留其他已填内容。

**Given** 入驻数据被处理
**When** 数据进入数据库、日志或页面
**Then** 密码使用自适应单向哈希，手机号加密保存
**And** 日志不含完整手机号或密码。

**Given** 客户仅使用键盘或缩放至 200%
**When** 完成注册
**Then** 字段、错误和提交操作仍可访问
**And** 焦点移至首个错误字段或提交结果。

### Story 1.3：客户查看并修正入驻审核结果

作为已提交申请的客户，
我希望查看当前审核状态和驳回原因，
以便了解进度或修正资料后重新提交。

**Acceptance Criteria:**

**Given** 申请为待审核
**When** 客户打开状态页面
**Then** 显示状态、提交时间、责任角色和反馈渠道
**And** 不显示虚假进度且不允许重复提交。

**Given** 申请被驳回
**When** 客户查看并修改允许字段后重新提交
**Then** 显示具体原因
**And** 状态由“驳回”变为“待审核”。

**Given** 申请已通过
**When** 客户查看状态
**Then** 显示进入平台的主要操作
**And** 不允许再次提交。

**Given** 待审核或驳回客户访问受保护功能
**When** 服务端处理请求
**Then** 拒绝访问
**And** 前端转到审核状态页并说明原因。

### Story 1.4：客户建立并终止安全登录会话

作为审核通过的客户，
我希望使用账号和密码安全登录及退出，
以便访问本企业的开放平台能力。

**Acceptance Criteria:**

**Given** 账号有效、审核通过且密码正确
**When** 客户登录
**Then** 建立 Redis 服务端会话并设置安全 Cookie
**And** 登录后进入平台总览。

**Given** 用户已登录
**When** 退出
**Then** 当前服务端会话立即失效
**And** 访问受保护页面须重新认证。

**Given** 同一账号 15 分钟内连续失败 5 次
**When** 再次登录
**Then** 锁定 30 分钟并显示人工渠道
**And** 不透露账号所属企业。

**Given** 同一 IP 15 分钟内失败 20 次
**When** 发起新登录
**Then** 拒绝 30 分钟并记录安全指标
**And** 不记录明文密码。

**Given** Redis 无法完成会话或锁定校验
**When** 登录或访问受保护能力
**Then** 系统失败关闭
**And** 返回暂时不可用提示和 Request ID。

### Story 1.5：内部人员受控审核及重置密码

作为商务专员或内部责任人，
我希望通过受控命令处理入驻审核和人工密码重置，
以便一期在没有运营后台时仍能安全、可追溯地运营。

**Acceptance Criteria:**

**Given** 商务专员取得待审核申请及资质材料
**When** 通过受控命令批准或驳回
**Then** 只允许合法状态转换，驳回必须填写原因
**And** 记录申请、结果、操作人、复核人和时间。

**Given** 操作跳过复核、状态非法或审计字段缺失
**When** 命令执行
**Then** 业务状态不变
**And** 返回明确失败原因。

**Given** 客户需要人工密码重置
**When** 授权责任人执行受控重置
**Then** 新密码以自适应哈希保存、锁定解除、现有会话失效
**And** 审计包含操作人、复核人、原因、前后状态和时间。

**Given** 未授权账号尝试审核或重置
**When** 操作执行
**Then** 操作被拒绝
**And** 不能以任意 SQL 绕过状态机。

### Story 1.6：平台建立企业租户安全上下文

作为平台安全负责人，
我希望所有企业范围访问都从已认证会话取得可信租户上下文，
以便客户参数无法扩大数据访问范围，后续业务能力能够复用统一隔离边界。

**Acceptance Criteria:**

**Given** 客户已建立有效登录会话
**When** 访问企业范围内的账号、申请或状态数据
**Then** 服务端从会话取得唯一企业标识
**And** 数据访问必须包含该企业范围。

**Given** 请求参数包含其他企业标识
**When** 服务端处理请求
**Then** 参数不得覆盖或扩大安全上下文
**And** 响应不得包含其他企业数据。

**Given** 客户尝试访问其他企业资源
**When** 请求被处理
**Then** 请求被拒绝且返回数据为零
**And** 记录企业、资源、时间和 Request ID。

**Given** 后续模块需要访问企业数据
**When** 接入账号、应用、权限或统计能力
**Then** 必须复用同一租户安全上下文
**And** 不得自行信任客户端企业标识。

### Story 1.7：平台记录不可变的关键操作审计

作为安全及合规责任人，
我希望关键状态变更形成完整且不可变的审计事件，
以便审核、重置及后续敏感操作能够被准确追溯。

**Acceptance Criteria:**

**Given** 登录锁定、入驻审核或人工密码重置发生
**When** 业务状态提交
**Then** 同一事务写入对应的封闭类别审计事件
**And** 不得使用开放式“其他”类别替代。

**Given** 审计事件写入
**When** 校验记录
**Then** 操作对象、操作人、复核人、前后状态、原因、时间及适用的 Request ID 完整率为 100%。

**Given** 业务事务回滚
**When** 状态变更失败
**Then** 状态与对应审计均不提交。

**Given** 普通账号尝试更新或删除审计事件
**When** 操作执行
**Then** 操作被拒绝
**And** 审计存储保持仅追加。

**Given** 后续 Epic 首次引入密钥重置、应用状态、权限、生产启用或额度调整
**When** 对应 Story 验收
**Then** 必须扩展明确审计类别并复用本 Story 的完整字段与事务规则。

**Given** 审计记录达到保留策略边界
**When** 执行归档
**Then** 自发生时间起至少保留一年且在保留期内可检索。

### Story 1.8：授权人员查询审计与状态历史

作为授权内部人员，
我希望按统一条件查询审计事件和状态转换历史，
以便核验审核责任、操作原因和关键状态变化。

**Acceptance Criteria:**

**Given** 授权人员查询审计
**When** 按对象、人员、时间或操作类型筛选
**Then** 返回匹配事件并按时间稳定排序。

**Given** 查看入驻申请或账号状态历史
**When** 打开变更详情
**Then** 显示合法转换、前后状态、原因、操作人、复核人、时间及适用的 Request ID。

**Given** 结果包含个人信息或技术敏感信息
**When** 展示
**Then** 手机号按前三后四脱敏
**And** 不展示密钥、完整签名、完整地址或内部堆栈。

**Given** 未授权人员尝试查询
**When** 请求被处理
**Then** 请求被拒绝并产生审计
**And** 不泄露记录是否存在。

**Given** 查询保留范围
**When** 页面展示可用期限
**Then** 明确区分至少一年审计记录与至少 90 天调用日志。

## Epic 2：客户创建并安全管理对接应用

客户获得可管理的系统对接身份，安全取得并轮换沙箱与生产凭证。

### Story 2.1：客户创建并查看本企业应用

作为审核通过的客户，
我希望创建和查看本企业的对接应用，
以便管理系统接入身份。

**Acceptance Criteria:**

**Given** 客户已登录且审核通过
**When** 提交有效应用名称和用途
**Then** 创建归属当前企业的运行中应用并生成唯一不可猜测 AppID。

**Given** 客户未通过审核
**When** 创建应用
**Then** 服务端拒绝且不创建应用或凭证。

**Given** 客户查看应用列表
**When** 返回数据
**Then** 只显示本企业应用、AppID、环境、状态和更新时间
**And** 不返回其他企业应用或任何 AppSecret。

**Given** 客户请求其他企业应用详情
**When** 服务端处理
**Then** 按不存在或无权访问处理
**And** 不泄露应用是否存在。

### Story 2.2：安全交付仅首次可见的沙箱密钥

作为应用所有者，
我希望在创建应用后安全取得沙箱 AppSecret，
以便开始沙箱对接，同时避免密钥长期暴露。

**Acceptance Criteria:**

**Given** 应用创建成功
**When** 生成沙箱 AppSecret
**Then** 使用安全随机源并以信封加密持久化
**And** 明文仅随本次响应返回一次。

**Given** 页面收到首次密钥
**When** 显示凭证
**Then** 标识沙箱和仅展示一次，并分别提供复制操作
**And** 密钥不进入 URL、Query Cache、localStorage、日志或监控事件。

**Given** 用户未确认保存
**When** 离开页面
**Then** 提示离开后不可再次查看
**And** 可取消离开继续复制。

**Given** 用户已离开首次展示页
**When** 再次查看凭证
**Then** 只显示掩码和重置入口
**And** 后端及普通运维不能返回明文。

**Given** 用户使用键盘或读屏
**When** 复制并确认保存
**Then** 全流程可完成且结果可被辅助技术感知。

### Story 2.3：隔离管理沙箱与生产凭证

作为应用所有者，
我希望明确区分沙箱和生产凭证，
以便避免将测试配置误用于生产。

**Acceptance Criteria:**

**Given** 应用未通过生产验收
**When** 查看凭证
**Then** 沙箱凭证可用，生产凭证显示未启用及前置条件。

**Given** 技术负责人批准生产访问
**When** 生成生产凭证
**Then** 生产与沙箱凭证不同，生产密钥仅首次展示
**And** 两环境使用独立权限、限流和 Redis 命名空间。

**Given** 页面呈现环境
**When** 客户查看或复制凭证
**Then** 沙箱与生产同时使用指定语义色和文字
**And** 不只依赖颜色或前缀。

**Given** 跨环境使用凭证
**When** 平台校验
**Then** 拒绝并返回稳定错误码和 Request ID。

### Story 2.4：客户停用和恢复应用

作为应用所有者，
我希望自主停用不再使用或存在风险的应用，
以便立即阻止其继续调用。

**Acceptance Criteria:**

**Given** 应用运行中
**When** 客户选择停用
**Then** 危险确认说明对象、环境和影响
**And** 确认后状态改为已停用。

**Given** 应用已停用
**When** 任一环境凭证发起新调用
**Then** 一分钟内拒绝且不访问数据中台。

**Given** 客户查看已停用应用
**When** 页面加载
**Then** 显示状态、停用时间和恢复入口。

**Given** 客户恢复应用且前置条件满足
**When** 提交操作
**Then** 状态恢复运行中，未失效凭证按环境恢复可用。

**Given** 恢复条件不满足
**When** 尝试恢复
**Then** 拒绝并说明阻塞项。

**Given** 应用状态变化
**When** 事务提交
**Then** 原子记录操作人、原因和前后状态审计。

### Story 2.5：客户安全轮换应用密钥

作为应用所有者，
我希望重置 AppSecret 并在 24 小时内平滑切换，
以便处理密钥遗失或泄露而不中断正常调用。

**Acceptance Criteria:**

**Given** 客户发起重置
**When** 显示确认
**Then** 说明环境、24 小时并行和旧密钥失效时间
**And** 高影响场景要求输入应用名称。

**Given** 客户确认
**When** 生成新密钥
**Then** 新密钥立即生效，旧密钥保留 24 小时
**And** 新密钥仅本次明文展示。

**Given** 轮换中
**When** 查看轮换面板
**Then** 显示可用密钥数量、失效时间和验证状态，不显示明文。

**Given** 新密钥验证失败
**When** 请求被拒绝
**Then** 旧密钥窗口内仍可用，并提供错误码、Request ID 和建议。

**Given** 客户提前终止或窗口到期
**When** 旧密钥再次调用
**Then** 拒绝且不进入业务处理。

**Given** 并发重置或终止发生版本冲突
**When** 提交状态转换
**Then** 仅一个转换成功，其余收到可恢复冲突提示。

**Given** 密钥状态变化完成
**When** 事务提交
**Then** 审计与密钥元数据原子提交，记录环境、操作人、原因、起止和 Request ID。

### Story 2.6：隔离沙箱与生产的运行边界

作为应用所有者和平台运维，
我希望沙箱与生产的全部运行边界清晰隔离，
以便测试行为、模拟数据和非生产密钥不会影响或进入生产环境。

**Acceptance Criteria:**

**Given** 沙箱与生产环境可用
**When** 检查运行配置
**Then** 域名、凭证、权限、数据、限流、操作记录和 Redis 命名空间相互独立
**And** 生产密钥不得进入非生产环境。

**Given** 客户访问沙箱
**When** 查询或调试接口
**Then** 只返回符合 v1 契约的模拟数据
**And** 不包含真实个人信息、生产客户标识或真实订单。

**Given** 页面发起调试请求
**When** 构造或发送请求
**Then** 只能选择沙箱目标
**And** 不存在可由客户端篡改为生产地址的目标参数。

**Given** 使用跨环境凭证、权限或标识
**When** 平台校验请求
**Then** 请求被拒绝并返回稳定错误与 Request ID
**And** 不访问目标环境的数据源。

**Given** 运维检索调用或操作记录
**When** 查看结果
**Then** 每条记录明确标识环境
**And** 不将沙箱与生产记录意外合并。

## Epic 3：客户申请接口权限并完成生产准备

应用取得明确、最小化的接口授权范围，并在身份绑定和联调验收通过后安全启用生产访问。

### Story 3.1：客户查看并提交逐接口权限申请

作为应用所有者，
我希望查看可申请接口并逐项提交权限申请，
以便只申请业务对接所需的数据能力。

**Acceptance Criteria:**

**Given** 客户进入权限页面
**When** 页面加载
**Then** 显示三个接口及用途、数据范围、敏感提示、要求和状态。

**Given** 接口未申请或已驳回
**When** 填写原因并提交
**Then** 为应用与接口创建独立待审核申请
**And** 驳回记录保留历史原因和重提记录。

**Given** 接口已通过或待审核
**When** 重复提交
**Then** 拒绝并说明当前状态和下一步。

**Given** 客户提交其他企业 applicationId
**When** 服务端处理
**Then** 拒绝且不创建申请。

**Given** 客户仅使用键盘
**When** 选择、填写并提交
**Then** 全流程可操作且错误与字段关联。

### Story 3.2：客户查看权限审核状态和原因

作为应用所有者，
我希望查看每个接口申请的审核状态和原因，
以便等待处理或修正申请。

**Acceptance Criteria:**

**Given** 应用存在多个接口申请
**When** 查看权限页
**Then** 每项独立显示未申请、待审核、通过、驳回或已撤权
**And** 同时使用文字、图标和语义色。

**Given** 申请驳回
**When** 查看详情
**Then** 显示审核时间、责任角色、原因和重提入口
**And** 不显示内部敏感备注。

**Given** 权限通过
**When** 查看接口
**Then** 显示获准环境和生效时间且不可重复申请。

**Given** 权限撤销
**When** 查看状态
**Then** 显示已撤权、生效时间、原因和反馈渠道。

### Story 3.3：业务产品受控审批和撤销接口权限

作为业务产品，
我希望通过受控流程逐接口批准、驳回或撤销权限，
以便落实最小权限和数据开放范围。

**Acceptance Criteria:**

**Given** 待审核申请
**When** 受控批准
**Then** 仅该接口变为通过
**And** 记录企业、应用、接口、理由、操作人、复核人和时间。

**Given** 驳回申请
**When** 提交审核
**Then** 必须填写客户可理解原因并允许后续重提。

**Given** 已通过权限被撤销
**When** 操作完成
**Then** 新请求一分钟内被拒绝
**And** 记录撤权原因、人员和生效时间。

**Given** 缺少复核、理由、字段批准或状态非法
**When** 执行命令
**Then** 状态不变并返回原因。

**Given** 未授权人员或任意 SQL 修改权限
**When** 执行
**Then** 被权限或受控入口阻止且不绕过审计。

### Story 3.4：绑定企业应用与业务客户主数据

作为技术对接负责人，
我希望将企业和应用绑定到已核验的业务客户主数据范围，
以便后续查询只能访问授权客户的数据。

**Acceptance Criteria:**

**Given** 企业已通过审核
**When** 提交客户主数据绑定
**Then** 记录企业、应用、环境和客户范围
**And** 记录核验依据、操作人、复核人和时间。

**Given** 应用有两个环境
**When** 查看绑定
**Then** 环境分别维护，沙箱只绑定模拟标识。

**Given** 目标无效、冲突或缺少复核
**When** 绑定
**Then** 拒绝且不创建部分绑定。

**Given** 外部请求携带 customerId
**When** 建立安全上下文
**Then** 范围只来自 AppID 绑定，参数不能扩大范围。

**Given** 绑定变更
**When** 事务提交
**Then** 缓存一分钟内失效并审计前后范围。

### Story 3.5：批准并执行字段开放清单

作为产品负责人、业务产品和技术对接负责人，
我希望共同批准并执行版本化字段开放清单，
以便平台只输出已批准且符合最小必要原则的数据字段。

**Acceptance Criteria:**

**Given** 接口准备进入生产联调
**When** 三方共同批准字段范围
**Then** 记录数据分类分级、字段清单、处理目的、留存规则和版本
**And** 缺少任一批准记录时生产准备不得完成。

**Given** 数据中台返回契约外或未批准字段
**When** 平台映射响应
**Then** 默认不输出该字段
**And** 不得进行通用字段透传。

**Given** 请求包含扩大字段范围的参数
**When** 平台处理请求
**Then** 参数不得绕过已批准字段清单。

**Given** 字段清单发生更新
**When** 新版本生效
**Then** OpenAPI、字段映射、示例和验证基线保持一致
**And** 记录变更内容、批准人、复核人和生效时间。

**Given** 数据中台字段或枚举发生不兼容变化
**When** 兼容性检查发现删除 v1 必填字段、改变字段类型或既有枚举语义
**Then** 阻止该变化进入 v1
**And** 要求通过新的 API 主版本处理。

### Story 3.6：客户与技术负责人查看生产准备状态

作为应用所有者和技术对接负责人，
我希望查看生产接入基础准备是否完成，
以便在进入沙箱联调前明确缺失项，同时不提前启用生产访问。

**Acceptance Criteria:**

**Given** 客户或技术负责人打开生产准备清单
**When** 页面加载
**Then** 显示企业审核、应用状态、身份绑定、逐接口权限、字段批准和限流配置
**And** 每项显示状态、责任角色和下一步。

**Given** 任一基础准备项缺失、冲突或不可追溯
**When** 计算准备状态
**Then** 状态保持阻塞并列出具体缺失项
**And** 不生成生产凭证或启用生产访问。

**Given** 全部基础准备项完成
**When** 状态重新计算
**Then** 应用进入“待联调/待最终验收”状态
**And** 不要求尚未交付的沙箱联调或生产查询证据。

**Given** 客户查看阻塞或准备完成状态
**When** 页面展示
**Then** 提供原因、责任角色、反馈渠道和后续联调入口
**And** 不暴露内部数据库或安全实现细节。

**Given** 准备状态发生变化
**When** 事务提交
**Then** 审计记录对象、人员、前后状态、原因、时间和 Request ID。

## Epic 4：客户依据统一文档完成沙箱联调

客户可以依据唯一契约理解接口、实现签名，并在不接触生产数据的情况下完成可诊断的在线调试。

### Story 4.1：建立三个接口的 OpenAPI v1 契约基线

作为平台开发与客户开发者，
我希望三个开放接口拥有唯一、可验证的契约，
以便文档、沙箱、生产和客户端不会产生语义漂移。

**Acceptance Criteria:**

**Given** v1 契约存在
**When** 校验运行
**Then** 包含三个端点、权限、安全、数据模型、分页和错误
**And** 类型、必填、可空、枚举、脱敏和示例完整。

**Given** 订单列表契约
**When** 查看参数
**Then** 范围最大31天、默认100、最大500、固定排序和不透明游标均已定义。

**Given** 安全契约
**When** 查看认证
**Then** 包含 AppID、HMAC、规范化请求、摘要、±5分钟和 nonce
**And** 沙箱与生产语义相同。

**Given** 契约变更
**When** CI 运行
**Then** 执行语法、示例和破坏性变更检查
**And** 漂移或不兼容变更阻止合并。

### Story 4.2：客户浏览完整且可导航的 API 文档

作为客户开发者，
我希望集中浏览接口用途、参数、响应、错误和治理规则，
以便独立完成对接开发。

**Acceptance Criteria:**

**Given** 客户进入文档
**When** 页面加载
**Then** 展示三个接口完整信息且来自 OpenAPI。

**Given** 客户查看公共规则
**When** 展开章节
**Then** 显示签名、防重放、分页、增量、限流、重试和错误说明。

**Given** 从错误或权限页进入
**When** 打开文档
**Then** 定位对应章节且返回后保留原状态。

**Given** 桌面端浏览
**When** 空间允许
**Then** 使用接口导航、正文和页内目录，技术标识可复制。

**Given** 平板或移动端
**When** 空间不足
**Then** 导航折叠且正文、参数表和错误说明可读。

**Given** 键盘或读屏用户
**When** 浏览文档
**Then** 目录、折叠、复制和切换可操作，标题与表格语义正确。

### Story 4.3：提供可复现的签名与调用示例

作为客户开发者，
我希望获得至少一种可直接复现的签名和调用示例，
以便快速验证自己的签名实现。

**Acceptance Criteria:**

**Given** 查看签名示例
**When** 选择订单列表
**Then** 展示原始请求、规范化内容、摘要、时间戳、nonce、待签名串和签名
**And** 中间结果可复制。

**Given** 使用固定测试向量
**When** 按说明计算
**Then** 结果与文档一致且后端契约测试复用同一向量。

**Given** 参数、时间戳或请求体变化
**When** 查看说明
**Then** 明确影响签名的内容及常见失败原因。

**Given** 示例需要密钥
**When** 渲染
**Then** 仅使用测试密钥且不含真实数据。

### Story 4.4：客户通过页面向沙箱发送测试请求（一期 1.x）

作为客户开发者，
我希望在控制台构造并发送沙箱请求，
以便快速验证参数、权限和签名结果。

**Acceptance Criteria:**

**Given** 客户选择应用和接口
**When** 打开调试器
**Then** 表单从 OpenAPI 构建，始终标识沙箱且无生产切换。

**Given** 参数无效或超限
**When** 发送
**Then** 就近显示错误且不发请求。

**Given** 参数有效且有权限
**When** 发送
**Then** 仅请求沙箱并以模拟数据返回契约结果。

**Given** 权限或凭证无效
**When** 发送
**Then** 执行与生产相同的权限和签名语义并返回 Request ID。

**Given** 移动端调试
**When** 宽度不足
**Then** 参数、请求和响应通过 Tabs 展示且无横向溢出。

### Story 4.5：客户理解沙箱响应并从错误中恢复（一期 1.x）

作为客户开发者，
我希望查看结构化响应和具体修复建议，
以便不依赖内部人员解决常见调试失败。

**Acceptance Criteria:**

**Given** 请求成功
**When** 展示结果
**Then** 显示 HTTP、业务码、耗时、Request ID 和 JSON，且可复制。

**Given** 签名、过期、nonce、权限或限流错误
**When** 展示
**Then** 使用统一错误并提供原因、重试性、动作和文档入口。

**Given** 模拟下游超时、不可用或过期
**When** 返回
**Then** 使用对应错误码且无部分成功数据。

**Given** 网络失败或页面超时
**When** 请求结束
**Then** 区分网络与协议错误并保留参数。

**Given** 请求状态变化
**When** 辅助技术读取
**Then** 结果被播报且焦点管理不打断阅读。

## Epic 5：客户安全获取供应链查询数据

客户通过受鉴权、授权、限流和租户隔离保护的生产 API，可靠查询客户资料与订单数据。

### Story 5.1：建立开放 API 安全请求上下文

作为获准应用的开发者，
我希望平台正确验证 AppID 和请求签名，
以便只有真实、未重放且状态有效的应用请求进入业务处理。

**Acceptance Criteria:**

**Given** 请求到达生产 API
**When** 凭证、签名、时间和 nonce 有效
**Then** 校验完整规范化请求并建立企业、应用、环境、客户范围和权限上下文
**And** 不向业务模块传递密钥或签名。

**Given** 签名无效、过期或 nonce 重复
**When** 校验
**Then** 拒绝并返回稳定错误和 Request ID，且不进入业务或数据中台。

**Given** 账号、应用、环境、权限或准入无效
**When** 校验
**Then** 默认拒绝且变化一分钟内生效。

**Given** Redis 无法校验 nonce 或状态
**When** 新请求到达
**Then** 失败关闭且不绕过安全检查。

**Given** 产生安全日志
**When** 写入
**Then** 不含密钥、完整签名或业务响应，且保留 Request ID/Trace ID。

### Story 5.2：应用查询绑定范围内的客户基础信息

作为获准应用的开发者，
我希望查询当前绑定客户的基础资料，
以便同步合作客户信息。

**Acceptance Criteria:**

**Given** 应用具备权限及准入
**When** 查询绑定客户
**Then** 从数据中台取得权威数据并返回 Customer 契约及 Request ID。

**Given** 客户标识不在授权范围
**When** 查询
**Then** 拒绝并审计，不扩大下游范围且不泄露资源存在性。

**Given** 响应含手机号
**When** 返回
**Then** 仅保留前三后四且日志无完整号码。

**Given** 字段未获批准
**When** 映射响应
**Then** 不输出且查询参数不能绕过。

### Story 5.3：应用按增量条件查询订单列表

作为获准应用的开发者，
我希望按时间范围或更新时间增量查询订单列表，
以便持续同步订单变化。

**Acceptance Criteria:**

**Given** 应用具备权限
**When** 提交不超过31天的有效范围
**Then** 返回 OrderSummary，默认页100、最大500。

**Given** 范围、页大小或时间无效
**When** 请求
**Then** 参数错误且不访问数据中台。

**Given** 响应含金额、时间和枚举
**When** 序列化
**Then** 使用十进制字符串、ISO 8601 和稳定枚举代码。

**Given** 无权限或范围不匹配
**When** 查询
**Then** 拒绝且不返回订单。

### Story 5.4：应用稳定分页遍历订单

作为同步程序开发者，
我希望通过稳定游标分页遍历订单，
以便并发更新期间不会永久遗漏或重复计入符合条件的数据。

**Acceptance Criteria:**

**Given** 固定条件存在多页
**When** 获取第一页
**Then** 按 `updatedAt ASC, orderId ASC` 排序并返回不透明游标。

**Given** 使用上一页游标
**When** 获取下一页
**Then** 从末尾排序键之后读取且客户端无需解析。

**Given** 分页中发生更新
**When** 持续增量同步
**Then** 数据不永久遗漏且相同时间以 orderId 稳定排序。

**Given** 游标篡改、过期或条件不匹配
**When** 请求
**Then** 返回参数错误且不执行未限定查询。

**Given** 运行一致性测试
**When** 插入更新边界数据
**Then** 验证无永久遗漏且重复可按稳定标识处理。

### Story 5.5：应用查询订单详情并保持列表一致

作为获准应用的开发者，
我希望按订单标识查询详情，
以便取得与订单列表一致的完整业务信息。

**Acceptance Criteria:**

**Given** 有权限且订单属授权客户
**When** 查询详情
**Then** 返回 OrderDetail 和 Request ID。

**Given** 订单不属授权客户
**When** 请求
**Then** 拒绝且不泄露存在性。

**Given** 同一订单出现在列表和详情
**When** 比较指定字段
**Then** 保持一致且同步延迟不超过5分钟。

**Given** 详情含手机号和地址
**When** 返回
**Then** 手机号前三后四，地址仅省市，其余为 `***`。

**Given** 每日一致性任务运行
**When** 抽样至少100单或不足时全量
**Then** 指定字段不一致率为0且异常进入监控。

### Story 5.6：平台正确处理数据中台故障和数据过期

作为客户开发者，
我希望平台准确区分数据中台超时、不可用和数据过期，
以便决定是否及何时重试，而不会使用错误数据。

**Acceptance Criteria:**

**Given** 下游请求超过3秒
**When** 超时
**Then** 之后1秒内返回 `DOWNSTREAM_TIMEOUT` 和 Request ID，无部分结果。

**Given** 下游连接失败或不可用
**When** 查询
**Then** 返回 `DOWNSTREAM_UNAVAILABLE`，按策略标记重试性，不返回旧缓存。

**Given** 新鲜度超过5分钟
**When** 查询
**Then** 返回 `DATA_STALE` 而非成功。

**Given** 幂等查询遇到瞬时网络错误
**When** 可重试
**Then** 仅在总预算内有限重试，4xx、业务和时效错误不重试。

**Given** 每分钟采集下游指标
**When** 指标异常
**Then** 两分钟内标明故障来源并覆盖三个接口。

### Story 5.7：按应用和接口执行基础限流

作为平台及正常调用客户，
我希望每个应用和接口按独立额度控制请求频率，
以便单个调用方超额时能够立即被限制且不占用下游容量。

**Acceptance Criteria:**

**Given** 请求未超过合法额度
**When** 调用到达
**Then** 按应用、接口和环境计算默认 60 RPM 与突发 20
**And** 请求继续进入后续处理。

**Given** 请求超过应用或接口额度
**When** 新请求到达
**Then** 返回 HTTP `429`、业务码 `RATE_LIMITED`、Request ID 和非负整数秒的 `Retry-After`
**And** 不访问数据中台。

**Given** 同一应用调用不同接口或环境
**When** 计算额度
**Then** 应用与接口额度可独立生效
**And** 沙箱与生产额度互不混用。

**Given** Redis 无法执行关键限流判断
**When** 新请求到达
**Then** 安全路径失败关闭
**And** 不以无限流状态继续访问数据中台。

### Story 5.8：受控批准和执行客户额度调整

作为业务产品和技术对接负责人，
我希望通过受控、可追溯的流程调整客户额度，
以便满足经批准的业务需求且不影响其他客户。

**Acceptance Criteria:**

**Given** 客户申请额度调整
**When** 业务产品批准且复核完成
**Then** 记录客户、应用、接口、原额度、新额度、生效时间、失效时间、原因、操作人和复核人。

**Given** 调整记录未批准、缺少复核或已经过期
**When** 平台计算限流
**Then** 该配置不得生效
**And** 使用当前合法额度。

**Given** 批准配置到达生效时间
**When** 新请求到达
**Then** 新额度按指定应用和接口生效
**And** 不要求中断其他客户服务。

**Given** 调整状态提交
**When** 事务完成
**Then** 配置与明确类别审计原子提交
**And** 审计字段遵循 Story 1.7。

### Story 5.9：保护数据中台容量并隔离异常客户

作为平台及正常调用客户，
我希望平台在接近下游容量时优先限制异常或超额应用，
以便数据中台和其他客户持续满足服务目标。

**Acceptance Criteria:**

**Given** 传递至数据中台的总请求达到 2,400 RPM
**When** 流量继续增长
**Then** 优先拒绝超过自身合法额度的应用请求
**And** 数据中台入口流量不得超过 3,000 RPM。

**Given** 单个应用以 5 倍默认额度持续请求 30 分钟
**When** 其他客户同时调用
**Then** 其他客户仍满足 P95≤1 秒、可用性≥99.9%折算目标及平台原因失败率≤1%。

**Given** 异常应用被限制
**When** 检查其余客户
**Then** 租户隔离、权限和调用结果保持正确
**And** 形成负载、限流、下游流量及其他客户指标证据。

### Story 5.10：验证生产查询性能、安全与只读边界

作为技术对接负责人和运维，
我希望验证查询接口在目标负载下仍满足性能、安全和只读约束，
以便为最终生产验收提供可复核证据。

**Acceptance Criteria:**

**Given** 20应用、3,000 RPM且样本充足
**When** 容量测试
**Then** 各接口P95不超过1秒，平台失败率不超过1%。

**Given** 20应用按最大查询运行30分钟
**When** 测试结束
**Then** 满足性能、失败率和新鲜度目标。

**Given** 客户尝试生产写入
**When** 调用
**Then** 无可用写入端点且非法方法正确拒绝。

**Given** 跨客户、跨环境或越权测试
**When** 执行
**Then** 数据返回为0且失败被审计。

**Given** Redis、日志或监控故障
**When** 安全请求到达
**Then** 不绕过安全控制且关键依赖失败关闭。

### Story 5.11：技术负责人完成最终生产验收并启用访问

作为技术对接负责人，
我希望在全部业务、安全和联调证据具备后完成最终验收，
以便只为符合上线条件的应用启用受限生产访问。

**Acceptance Criteria:**

**Given** 应用申请最终生产验收
**When** 打开验收清单
**Then** 显示企业审核、应用状态、身份绑定、接口权限、字段批准、三个接口沙箱联调、限流、隔离、日志和容量证据
**And** 每项均可追溯至责任角色和证据。

**Given** 任一门槛缺失、失败或不可追溯
**When** 技术负责人尝试批准
**Then** 拒绝批准并显示阻塞项、原因、责任角色和重提条件
**And** 不生成生产凭证或启用生产访问。

**Given** 全部门槛通过且复核完成
**When** 技术负责人批准
**Then** 记录生产验收结论并生成与沙箱隔离的生产凭证
**And** 生产访问仅限批准接口、字段和客户范围。

**Given** 生产启用后的企业、应用、绑定或权限状态失效
**When** 状态变更生效
**Then** 受影响的新请求在一分钟内被拒绝
**And** 客户控制台显示阻塞原因和支持渠道。

**Given** 验收结论提交
**When** 事务完成
**Then** 状态、凭证元数据与审计原子提交
**And** 审计包含对象、操作人、复核人、检查结果、原因、时间和 Request ID。

## Epic 6：客户与技术团队掌握调用情况并完成故障闭环

客户可以掌握应用运行状况，内部技术团队可以通过统一标识和分类完成问题定位与反馈。

### Story 6.1：为调用生成统一事件和诊断分类

作为技术对接负责人，
我希望每次调用都拥有唯一标识和稳定结果分类，
以便统计与故障排查使用同一事实来源。

**Acceptance Criteria:**

**Given** 沙箱或生产请求进入平台
**When** 开始处理
**Then** 接受合法或生成唯一 Request ID，并贯穿全链路。

**Given** 请求到达终态
**When** 生成调用事件
**Then** 唯一分类为成功、鉴权失败、权限拒绝、限流、业务错误、超时或系统错误
**And** 同一 Request ID 只产生一个计数终态。

**Given** 事件异步处理
**When** 业务事务提交
**Then** 通过 Outbox 持久化且消费者幂等。

**Given** 记录事件或日志
**When** 写入
**Then** 包含环境、应用、接口、时间、结果和耗时，不含敏感内容。

**Given** 日志或监控故障
**When** 请求处理
**Then** 不绕过安全控制并产生内部健康指标。

### Story 6.2：聚合应用和接口调用统计

作为应用所有者，
我希望获得准确、及时的调用聚合数据，
以便判断应用是否正常运行。

**Acceptance Criteria:**

**Given** 调用事件已持久化
**When** 聚合执行
**Then** 按中国标准时间、小时、企业、应用、环境、接口和结果聚合
**And** 2xx且业务成功计成功，其余终态计失败且 Request ID 去重。

**Given** 新调用发生
**When** 聚合正常
**Then** 每小时刷新且两小时内可见。

**Given** 聚合重复或恢复
**When** 重处理
**Then** 结果幂等无重复。

**Given** 调用日志到期
**When** 归档
**Then** 排查日志至少90天且聚合数据不丢失。

### Story 6.3：客户查看近七天调用统计（按接口筛选为一期 1.x）

作为应用所有者，
我希望查看应用近七天调用总量、成功和失败情况，
以便快速判断运行状态。

**Acceptance Criteria:**

**Given** 客户进入统计页
**When** 选择本企业应用
**Then** 显示近7个自然日总量、成功、失败和成功率且不越租户。

**Given** 按接口筛选
**When** 选择接口
**Then** 只显示该接口且状态写入URL。

**Given** 首次加载或局部刷新
**When** 等待
**Then** 首次用 Skeleton，刷新保留已有数据。

**Given** 应用无调用
**When** 加载
**Then** 显示尚无调用及调试入口，不显示误导成功率。

**Given** 筛选无结果
**When** 显示
**Then** 与无调用区分且提供清除筛选。

**Given** 展示趋势
**When** 查看
**Then** 同时有文字摘要和精确值且不只依赖颜色。

### Story 6.4：内部人员按统一条件检索调用记录

作为技术对接负责人或运维，
我希望按 Request ID 和调用上下文检索内部记录，
以便快速确定故障阶段和影响范围。

**Acceptance Criteria:**

**Given** 授权人员有 Request ID
**When** 检索
**Then** 返回入口、安全、业务和下游关联记录及关键上下文。

**Given** 不知道 Request ID
**When** 按应用、接口、环境、时间和错误组合查询
**Then** 返回匹配记录。

**Given** 结果含敏感字段
**When** 显示
**Then** 个人信息脱敏且密钥、签名、响应体和内部堆栈不可见。

**Given** 未授权人员检索
**When** 请求
**Then** 拒绝并审计且不泄露存在性。

**Given** 记录在90天内
**When** 检索
**Then** 可用；超出范围明确说明。

### Story 6.5：技术负责人记录并闭环客户问题

作为技术对接负责人，
我希望记录客户问题、排查过程、处置结果和反馈状态，
以便通过企业微信或邮件形成可追溯的故障闭环。

**Acceptance Criteria:**

**Given** 客户反馈问题
**When** 登记
**Then** 记录企业、应用、环境、接口、时间、Request ID、描述和渠道，缺失信息提示补充。

**Given** 常见客户错误
**When** 处理
**Then** 记录原因、建议并更新为待客户验证。

**Given** 需升级运维
**When** 升级
**Then** 传递完整上下文并记录过程、影响、来源和结果。

**Given** 客户确认恢复
**When** 关闭
**Then** 记录反馈时间、最终状态和 Request ID，并保留历史。

**Given** 问题含个人信息
**When** 保存展示
**Then** 最小化脱敏且不复制未筛选原文到日志。
