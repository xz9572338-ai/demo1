# 入驻审核受控操作

仅商务专员可发起，另一名授权人员复核。执行 `onboarding-review.sql` 时绑定申请公开编号、`APPROVED|REJECTED`、原因、操作人和复核人；不得拼接 SQL。

执行工具必须开启事务，并分别检查 `application_rows = 1`、`account_rows = 1`；任一结果不是 1 时立即回滚，两者均为 1 且双表状态一致后才提交。操作人与复核人不得相同。驳回必须填写客户可理解且不含敏感信息的原因；通过时原因必须为空。保存工单编号、参数摘要、执行时间和复核证据，不保存密码、手机号明文或密钥。
